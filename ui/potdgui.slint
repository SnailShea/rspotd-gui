import { VerticalBox } from "std-widgets.slint";
export component PotDGUI inherits Window {
    VerticalBox {
        Text {
            text: "Password of the Day";
        }

        Text {
            text: "Test";
        }
    }
}
